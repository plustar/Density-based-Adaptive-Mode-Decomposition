# experiments/mdamd_paper/__init__.py
"""Experiments reproducing the MDAMD paper's §VIII.

The high-dimensional / projection experiments (what the draft called
'GDAMD' internally) live here too — the public API treats them as
special cases of the same pipeline with ``r < d``.
"""
